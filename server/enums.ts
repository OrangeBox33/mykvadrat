export enum EMessageTypes {
	GET_GRID = 'getGrid',
	DRAW = 'draw',
	GET_CHAT = 'getChat',
	SEND_TO_CHAT = 'sendToChat',
}

export enum EArduinoActions {
	UPDATE = 0,
	GRID = 1,
	DRAW = 2,
	HISTORY = 3,
}
